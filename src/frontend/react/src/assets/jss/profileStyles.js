const profileStyles = {
  profileContainer: {
    fontSize: "16px",
    textAlign: "center",
    color: "red",
  },
};

//here the profileContainer is a class name that we are going to apply for div element in Profile component
export default profileStyles;
